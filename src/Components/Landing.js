import React from 'react';

const Landing = () => {
    return(
        <div style={{backgroundColor: 'black'}}>
            <button style={{color: 'white'}}>Go to App!</button>
        </div>
    )
}

export default Landing;